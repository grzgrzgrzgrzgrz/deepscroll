# LLM credentials
# Fill only the provider keys you actually use.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional runtime defaults for the MCP server and library integration.
RLM_LLM_PROVIDER=openai
RLM_LLM_MODEL=gpt-5.4-mini
RLM_MAX_TOKENS=4096
RLM_TEMPERATURE=0.2
